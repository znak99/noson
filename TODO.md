# Noson TODO

이 문서는 사용자와 합의된 프로젝트 작업의 상태를 관리한다. 등록, 우선순위 및 상태 이동 규칙은 `docs/TODO_GUIDE.md`를 따른다.

## 미대응

1. **대체 시장 데이터 공급자 사용 가능성 및 데이터 계약 검증**
   - 구체적인 설명: Coincheck Public API를 첫 후보로 삼아 제3자 Android·iOS 앱의 시세 재표시·가공·캐시와 Cloudflare 정기 조회가 허용되는지 공식 문서와 필요한 서면 문의로 확인한다. `BTC`, `ETH`, `XRP`, `SOL`, `DOGE` 지원, REST·WebSocket·1분봉, 수수료·주문 규칙, 호출 제한과 무료 운영 가능성을 검증하고 실제 연동 전에 주 데이터 공급자와 데이터 계약을 확정한다.
2. **Public REST 시장 데이터 클라이언트 구현**
   - 구체적인 설명: 시장 데이터 제공자를 교체할 수 있는 경계 아래 status, ticker, symbols 및 1분봉 응답을 조회하고 문자열 수치를 정밀도 손실 없이 변환하는 REST 클라이언트를 구현한다. 일본시간 06:00 일자 전환, 거래 없는 분의 캔들 누락, 잘못된 응답, API 오류, 타임아웃과 오프라인 상태를 fixture 기반 단위 테스트로 검증하며 실제 제공자는 공식 이용 조건 확인 결과에 따라 확정한다.
3. **Public WebSocket 실시간 시세와 재연결 구현**
   - 구체적인 설명: REST 초기값 이후 5개 지원 종목의 Bid, Ask와 Last 데이터를 순차 구독하고 Riverpod 상태로 제공한다. 구독 속도 제한, ping/pong, 연결 종료, 지수 백오프 재연결, 중복·역순 이벤트와 REST/WebSocket 정합성 정책을 구현하고 연결 상태 및 오프라인 복구를 테스트한다.
4. **종목 목록과 상세 차트 화면 구현**
   - 구체적인 설명: `BTC`, `ETH`, `XRP`, `SOL`, `DOGE`의 현재가, 24시간 변동률, 고가, 저가, 거래량과 차트를 표시하는 일본어 종목 목록 및 상세 화면을 구현한다. 로딩, 정상, 오래된 데이터, 빈 캔들, 오프라인과 오류 상태, 매수·매도 화면 진입을 위젯 테스트와 대상 플랫폼 렌더링으로 검증한다.
5. **시장가 주문과 로컬 자산 반영 구현**
   - 구체적인 설명: 시장가 매수는 현재 Ask, 시장가 매도는 현재 Bid로 즉시 체결하고 Taker 수수료를 적용하는 로컬 주문 흐름을 구현한다. 주문 금액과 수량, 최소 단위, 가용 JPY 및 보유 수량을 검증하고 잔액, 보유량, 평균 매수가와 거래 내역을 하나의 원자적 처리로 갱신하며 중복 요청, 경계값, 잔액 부족 및 저장 실패 테스트를 작성한다.

## 진행중

## 완료

- **최초 실행 온보딩과 가상자금 초기화 구현**
  - 구체적인 설명: 앱이 실제 금융거래나 투자 추천을 제공하지 않고 모든 자산과 손익이 가상 데이터임을 알리는 일본어 온보딩을 구현한다. 익명 Installation ID를 생성하고 최초 가상자금 ¥1,000,000을 한 번만 지급하며, 재실행과 온보딩 재진입에서도 중복 지급되지 않는지 데이터베이스 및 위젯 테스트로 검증한다.
  - 완료 내용: 앱 시작 시 Riverpod의 단일 Drift 데이터베이스와 Installation Repository를 통해 익명 UUID와 `¥1,000,000`을 원자적으로 초기화하는 시작 게이트를 구현했다. 최초 실행에는 실제 매매·입출금이 없는 시뮬레이션, 가상 자산·손익, 투자 조언·종목 추천·수익 보장 미제공을 알리는 일본어 단일 화면을 표시하고 `仮想取引を始める` 선택 시 완료 상태를 저장한 뒤 종목 탭으로 전환한다. 이후 실행에는 메인 셸로 바로 이동하며 초기화 실패에는 일본어 오류와 재시도 경로를 제공한다.
  - 검증: 데이터베이스 테스트에서 완료 후 재초기화해도 Installation과 JPY 잔액이 한 행씩 유지되고 온보딩 완료 상태와 `¥1,000,000` 잔액이 보존됨을 확인했다. 위젯 테스트에서 최초 고지, 완료 전이, 재실행 시 온보딩 미표시, 중복 지급 방지 및 초기화 실패 후 복구를 검증했다. `dart format lib test`, `flutter analyze`, 전체 35개 테스트와 `flutter build apk --debug`가 성공했다. iOS 빌드와 실제 기기 렌더링은 현재 Linux 환경에서 실행할 수 없어 기존 출시 게이트로 유지한다.
- **GMO Coin Public API 제3자 이용 조건 공식 문의 및 답변 반영**
  - 구체적인 설명: 준비된 일본어 문안을 GMO Coin 공식 문의 양식으로 제출하고, 제3자 모바일 앱의 시세 재표시·캐시·가공, Cloudflare Worker의 정기 1분봉 조회, 출처·상표 표시, Public REST 호출 제한 및 스토어 배포 조건에 대한 답변을 확보한다. 제출일과 문의 번호, 답변 내용을 `docs/GMO_COIN_API_VALIDATION.md`에 기록하고, 결과에 따라 `docs/SYSTEM_DESIGN_V1.md`에서 GMO Coin 채택을 확정하거나 대체 데이터 공급자 검토로 전환한다.
  - 완료 내용: 2026-08-13 22:09 JST에 비영리·무료 모의투자 앱, 계정·API Key 미연동, 제3자 앱 표시·가공·캐시와 Cloudflare 정기 조회 목적을 명시해 문의했고 2026-08-14 12:25 JST에 GMO Coin 지원팀의 답변을 받았다. 지원팀은 API를 계좌 개설 고객용 서비스라고 설명하고 상업 목적 앱에서 API 문서 이용을 삼가 달라고 요청했다. 제안한 배포 형태를 허용한 답변이 아니므로 GMO Coin REST·WebSocket을 미채택하고 대체 공급자 검토로 전환했으며 답변 요약과 판단을 `docs/GMO_COIN_API_VALIDATION.md`, 시스템 설계, Cloudflare 및 스토어 검증 문서에 반영했다.
  - 검증: 사용자가 제공한 공식 답변 화면에서 발신자와 답변 시각, 일본어 본문을 직접 확인했다. 답변 화면에는 문의 번호가 없음을 기록했고 이메일 주소와 이미지 원본은 저장소에 보관하지 않았다. 프로젝트 전체에서 조건부 GMO Coin 채택, GMO 전용 배포 게이트와 상표 문구를 검색해 공급자 중립 표현으로 갱신했으며 기술 검증 당시의 수치와 제약은 출시 기준이 아닌 과거 fixture임을 명시했다. `git diff --check`, TODO 형식과 `AGENTS.md`·`CLAUDE.md`의 바이트 단위 동일성을 확인했다.
- **Drift 로컬 데이터 스키마와 초기 마이그레이션 구현**
  - 구체적인 설명: Installation ID, 앱 설정, JPY 잔액, 보유 자산, 거래 내역과 주문 캐시를 저장할 Drift 스키마와 제약 조건을 설계한다. 스키마 버전과 초기 마이그레이션, 최초 가상자금의 원자적 생성 및 기존 데이터 보존 경로를 구현하고 정상, 중복 초기화, 제약 위반 및 마이그레이션 테스트를 작성한다.
  - 완료 내용: SQLite `STRICT` 테이블로 Installation, 범용 설정, JPY 잔액, 자산 포지션, 시장가·지정가 주문과 주문당 하나의 불변 체결 기록을 분리했다. `FixedDecimalConverter`로 모든 금융 수치를 10진 `TEXT`에 저장하고 시각은 UTC 마이크로초 `INTEGER`로 통일했다. 최초 UUID v4 Installation ID와 `¥1,000,000`을 원자적으로 한 번만 생성하는 Repository, 설정 Repository와 미체결 주문 차단·Installation ID 유지·generation 증가 방식의 원자적 포트폴리오 리셋을 구현했다. 명시적 전진 마이그레이션 정책, 버전 1 JSON 스냅샷과 생성 검증 코드를 `docs/LOCAL_DATABASE_DESIGN.md` 및 시스템 설계에 기록했다.
  - 검증: Flutter 3.41.9의 의존성 고정과 생성 도구 호환성을 확인해 Drift 런타임·`drift_dev 2.34.0`, `build_runner 2.15.1`을 사용했다. `dart run build_runner build`와 `drift_dev make-migrations`가 경고 없이 성공했고 버전 1 스냅샷이 현재 스키마와 일치했다. 중복·실패 초기화 롤백, 온보딩 상태, 제약 위반, 잘못된 10진 문자열, 설정 갱신, 리셋 차단과 데이터 보존, UUID 형식을 포함해 전체 33개 테스트가 통과했으며 `flutter analyze` 문제 없음과 Android 디버그 APK 빌드 성공을 확인했다.
- **Flutter 앱 기반 구조와 핵심 의존성 초기화**
  - 구체적인 설명: 기본 카운터 예제를 제거하고 Riverpod, Drift/SQLite, WebSocket, HTTP 및 차트 패키지의 필요성, 라이선스와 지원 플랫폼을 확인해 최소 의존성으로 추가한다. 기능 단위 디렉터리, 라우팅, Provider 경계와 일본어 앱 셸을 구성하고 포맷, 정적 분석, 기본 위젯 테스트 및 대상 플랫폼 빌드로 초기 구조를 검증한다.
  - 완료 내용: Android와 iOS만 출시·검증 대상으로 확정해 Linux, macOS, Web 및 Windows 플랫폼 파일을 제거했다. Flutter 기본 `Navigator`, 기능 중심 디렉터리, Riverpod `ProviderScope`와 일본어 `銘柄`·`資産`·`メニュー` 앱 셸을 구성하고 모든 탭에 모의투자 고지를 표시했다. 승인된 최소 의존성을 추가하고 버전, 라이선스, 필요성과 모바일 지원 결과를 `docs/FLUTTER_FOUNDATION_VALIDATION.md` 및 시스템 설계에 기록했다.
  - 검증: `dart format lib test`와 `flutter analyze`를 통과했고 기존 거래 계산 테스트를 포함한 23개 테스트가 모두 성공했다. `flutter build apk --debug`로 Android APK 생성을 확인했다. 현재 Linux 환경에서는 iOS 빌드를 실행할 수 없어 macOS·Xcode 환경의 무서명 빌드와 실제 기기 렌더링을 출시 게이트로 남겼으며, Android 도구의 SDK XML 버전 차이 경고도 출시 빌드 환경 확인 항목으로 기록했다.
- **Cloudflare 무료 한도와 지정가 주문 백엔드 타당성 검증**
  - 구체적인 설명: 5개 지원 종목의 1분봉 조회, 미체결 지정가 주문 처리 및 동기화에 필요한 Workers, D1과 Cron 사용량을 예상 사용자 수와 주문량별로 산정한다. 무료 한도에서의 CPU 시간, 일일 요청과 쿼리, Cron 개수, 저장 용량 및 한도 초과 동작을 검증하고, 개발비와 운영비 ¥0 목표를 만족하는 구성이나 대체 처리 방안을 설계 문서에 확정한다.
  - 완료 내용: 2026-08-14 공식 무료 한도와 초과 동작을 `docs/CLOUDFLARE_BACKEND_FEASIBILITY.md`에 기록하고, 단일 Cron이 매분 한 종목을 순환해 종목별 최대 약 5분 지연으로 누락 1분봉을 복구하는 구조를 확정했다. Installation ID당 미체결 주문 20개, 결과 확인 후 7일·미확인 90일 보존, 인덱스 기반 범위 처리, 70% 경고·80% 신규 주문 제한 검토 및 유료 자동 전환 금지를 시스템 설계에 반영했다. 활성 사용자당 하루 주문 2회·취소 1회·동기화 12회 가정에서 100 DAU와 1,000 DAU는 무료 범위에 여유가 있고 10,000 DAU는 Workers 요청과 D1 쓰기 한도를 초과한다고 판단했다.
  - 검증: Workers 요청, CPU, subrequest와 Cron 한도 및 오류 `1027`·`1102`, D1 읽기·쓰기·저장·쿼리 한도와 초과 시 쿼리 중단을 최신 공식 문서와 대조했다. 수식 재계산 결과 1,000 DAU에서 Worker 16,440요청/일, D1 247,440행 읽기/일·15,440행 쓰기/일, 미확인 결과 최악 가정 저장 약 195.3MiB로 각 무료 한도 이내임을 확인했다. 실제 CPU p95, 쿼리 인덱스 사용 및 D1 `meta` 측정은 백엔드 구현·출시 게이트로 명시했고, `git diff --check`, TODO 섹션·연속 번호·항목 형식과 `AGENTS.md`·`CLAUDE.md`의 바이트 단위 동일성을 확인했다.
- **앱 스토어 정책 및 일본 규제·표시 기준 사전 검증**
  - 구체적인 설명: 실제 거래와 자산 보관을 하지 않는 무료 암호화폐 현물 모의투자 앱을 기준으로 Apple App Store와 Google Play의 금융·암호화폐 앱 분류, 개발자 또는 법인 요건, 금융 기능 선언, 연령 제한, 개인정보 처리 및 필수 고지 사항을 최신 공식 자료로 확인한다. 일본 내 법적 분류가 불명확한 사항은 전문가 확인이 필요한 위험으로 구분하고, 확인일, 공식 출처, 출시 가능 조건 및 필요한 앱·스토어 문구를 문서화한다.
  - 완료 내용: 2026-08-13 기준 Apple, Google, 일본 금융청, 개인정보보호위원회 및 소비자청의 공식 자료로 조건부 출시 가능성을 검증하고 `docs/STORE_POLICY_VALIDATION.md`에 근거, 제출 체크리스트, 일본어 고지 초안과 잔여 위험을 기록했다. 실제 거래·보관·중개·추천을 금지하면 거래소·지갑 라이선스 대상이 아닐 가능성이 높다고 판단했다. 기존 개발자 계정 사용, `Finance` 카테고리, 성인 전용이 아닌 Google 13세 이상·Apple 설문 산출 등급, 직접 식별 정보 미수집과 익명 Installation ID·주문 데이터 최소 수집 및 정확한 스토어 신고 원칙을 확정해 시스템 설계에 반영했다.
  - 검증: 각 결론을 최신 공식 원문과 대조했고 Apple 암호자산·금융·개인정보·연령 정책, Google 금융 기능 신고·암호자산·계정·개인정보·등급 정책, 일본의 2026년 암호자산 중개 제도·개정법 시행 조건·개인관련정보·오인 표시 기준의 링크와 확인일을 기록했다. `git diff --check`, TODO 고정 섹션·연속 번호·항목 형식 및 `AGENTS.md`와 `CLAUDE.md`의 바이트 단위 동일성을 확인했다. 모의투자 예외에 대한 스토어 심사 재량과 2026년 개정법 시행 상태는 제출 직전 재확인 위험으로 명시했다.
- **금액·가격·수량 정밀도와 거래 계산 규칙 확정**
  - 구체적인 설명: 금액, 가격, 수량 및 수수료에 사용할 고정소수점 표현과 종목별 자릿수, 반올림, JPY 표시, Maker/Taker 수수료 부호, 평균 매수가, 실현·미실현 손익 및 주문 예약금 계산 규칙을 확정한다. 규칙을 설계 문서에 반영하고 정상값, 경계값 및 오류 조건을 검증하는 순수 Dart 단위 테스트를 작성한다.
  - 완료 내용: 12자리 고정소수점 `BigInt` 값 객체와 문자열 기반 API·DB 경계, 종목별 동적 거래 규칙 검증, 호가·수량 단위 불일치 거부 및 JPY 정수 표시를 구현했다. 시장가 Taker·미래 봉 지정가 Maker 수수료, 매수 취득원가, 비례 원가 배분 실현손익, Bid 기준 미실현손익과 Taker 수수료 포함 지정가 예약금 계산을 순수 Dart 도메인으로 분리했다. 공식 GMO Coin 수수료 단수 처리와 달리 모의투자에서는 정확한 소수 수수료를 유지하는 단순화 원칙을 설계 문서에 명시했다.
  - 검증: `dart format`을 적용했고 `flutter analyze`에서 문제 없음, `flutter test`에서 기존 위젯 테스트를 포함한 22개 테스트 통과를 확인했다. 5개 지원 종목의 최소·최대·호가·수량 단위, 잘못된 공급자 규칙, 잔액·수량 부족, Maker 리베이트, 부분·전량 매도와 예약금 경계값을 검증했다.
- **일반 Git 완료 절차 자동 수행 명문화**
  - 구체적인 설명: 합의된 작업과 검증이 끝난 뒤 일반 커밋, 일반 푸시, PR 생성 및 병합을 AI Agent가 별도 사용자 승인 없이 자동으로 수행하도록 공통 Agent 규칙과 Git 전략에 명시한다. 강제 푸시, 하드 리셋 및 강제 브랜치 삭제 같은 파괴적 작업의 승인 요건은 유지한다.
  - 완료 내용: `AGENTS.md`, `CLAUDE.md`, `docs/GIT_STRATEGY_S1.md`에 일반 Git 완료 절차의 자동 수행 원칙과 파괴적 작업 제외 범위를 추가했다.
  - 검증: 일반 Git 작업 승인 요구 문구가 없음을 전체 정책 문서에서 검색했고, 추가된 규칙의 내용과 적용 범위를 대조했으며 `AGENTS.md`와 `CLAUDE.md`의 바이트 단위 동일성을 확인했다.
- **GMO Coin Public API 사용 가능성 및 데이터 계약 검증**
  - 구체적인 설명: 최신 공식 API 문서와 약관을 기준으로 제3자 모바일 앱의 시세 표시 허용 범위를 확인하고, `BTC`, `ETH`, `XRP`, `SOL`, `DOGE`의 ticker, orderbook, 1분봉 응답과 호출 제한, 가격·수량 정밀도, 최소 주문 단위 및 Maker/Taker 수수료를 검증한다. 확인 결과와 필요한 문의 내용, 주 데이터 공급자 채택 여부 또는 대체 공급자 검토 조건을 문서화한다.
  - 완료 내용: 공식 자료와 2026-08-13 실응답을 기준으로 GMO Coin Public API가 MVP 5개 종목에 기술적으로 적합함을 확인했다. 거래 없는 분의 1분봉 누락, 일본시간 06:00 날짜 전환, WebSocket 순차 구독 및 REST 선초기화 요구를 식별했다. 제3자 앱의 재표시와 Cloudflare 정기 조회는 공개 자료만으로 허용 여부를 확정할 수 없어 조건부 주 데이터 공급자로 판단하고, `docs/GMO_COIN_API_VALIDATION.md`에 공식 출처, 데이터 계약, 종목별 주문 규칙·수수료, 일본어 문의 문안 및 대체 공급자 검토 조건을 기록했다. 관련 결정을 `docs/SYSTEM_DESIGN_V1.md`에도 반영했다.
  - 검증: Public REST의 status, ticker, orderbook, 당일 1분봉 및 symbols를 읽기 전용으로 호출해 5개 종목의 `status == 0`, 필수 필드, 정밀도, 최소·최대 주문 수량과 Maker/Taker 수수료를 확인했다. Public WebSocket에서는 초당 1회 구독 제한을 지켜 5개 종목 orderbooks의 Ask/Bid 스냅샷 수신을 확인했다. ticker는 45초 관찰 중 BTC만 수신되어 초기 스냅샷으로 사용할 수 없음을 기록했다. `git diff --check`, TODO의 3개 고정 섹션과 항목 형식, `AGENTS.md`와 `CLAUDE.md`의 바이트 단위 동일성을 확인했다. 제3자 재표시 허용과 Public REST 정량 제한은 공식 문의 답변이 필요한 잔여 위험으로 남겼다.
- **프로젝트 운영 문서 초기화 및 시스템 설계 재정리**
  - 구체적인 설명: `docs/SYSTEM_DESIGN_V1.md`를 AI Agent가 빠르게 해석할 수 있는 구조로 재작성하고, 동일한 `AGENTS.md`와 `CLAUDE.md`, 루트 `TODO.md`, `docs/TODO_GUIDE.md`를 작성한다.
  - 완료 내용: 시스템 설계를 결정 상태, 제품 범위, 거래 규칙, 컴포넌트 책임, 데이터 정책, 출시 확인 사항과 구현 전 결정 항목 중심으로 재구성했다. 공통 Agent 규칙과 TODO 생명주기 및 형식을 새 문서에 정의했다.
  - 검증: `AGENTS.md`와 `CLAUDE.md`의 바이트 단위 동일성, TODO의 고정 3개 섹션, 필수 정책 문서 참조, 목록 형식과 문서의 후행 공백 부재를 확인했다.
