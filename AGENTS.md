# Noson 프로젝트 AI Agent 작업 규칙

## 1. 목적과 적용 범위

이 문서는 Noson 프로젝트에서 작업하는 모든 AI Agent가 따라야 할 공통 규칙이다. 코드, 문서, 설정, 테스트, Git 및 TODO 관련 작업 전체에 적용한다.

`AGENTS.md`와 `CLAUDE.md`는 서로 다른 도구가 동일한 규칙을 읽도록 제공하는 복제 문서이다. 두 파일은 파일명만 다르며 내용은 항상 완전히 같아야 한다.

## 2. 지침 우선순위

지침이 충돌하면 다음 순서로 판단한다.

1. 사용자의 최신 명시적 요청
2. 프로젝트 루트의 `AGENTS.md`와 `CLAUDE.md`
3. `docs/SYSTEM_DESIGN_V1.md`
4. 작업별 정책 문서
5. 기존 코드와 일반적인 개발 관례

충돌을 임의로 해석하면 제품 원칙이나 데이터 정합성이 달라질 수 있는 경우 작업을 중단하고 사용자에게 확인한다. 새로운 사용자 결정으로 기존 문서가 낡게 되면 관련 문서를 같은 논리적 작업 안에서 갱신한다.

## 3. 필수 참조 문서

| 문서 | 역할 | 확인 시점 |
|---|---|---|
| `docs/SYSTEM_DESIGN_V1.md` | 제품 범위, 시스템 경계, 확정 사항 및 미결정 사항 | 모든 기능 설계와 구현 전 |
| `docs/GIT_STRATEGY_S1.md` | 브랜치, 커밋, PR 및 Git 안전 정책 | 모든 Git 작업 전 |
| `docs/TODO_GUIDE.md` | TODO 등록, 우선순위, 상태 이동 및 기록 방식 | 모든 TODO 작업 전 |
| `TODO.md` | 합의된 작업과 현재 진행 상태 | 작업 시작 전과 종료 전 |

문서를 일부만 추측해서 적용하지 않는다. 관련 작업을 수행하기 전에 해당 문서의 현재 내용을 직접 확인한다.

## 4. 작업 시작 절차

모든 작업은 다음 순서로 시작한다.

1. 사용자의 요청과 관련 문서를 읽고 작업 범위를 한 문장으로 정의한다.
2. `TODO.md`와 `docs/TODO_GUIDE.md`를 확인한다.
3. 사용자와 논의하여 결정된 작업이 TODO에 없으면 우선순위를 판단하고, 미대응 섹션의 몇 번에 등록할지 사용자에게 제안한다.
4. 실제 작업을 시작하기 직전에 대상 항목을 미대응에서 진행중으로 이동한다.
5. `git status`와 현재 브랜치를 확인하고 기존 변경을 파악한다.
6. Git 저장소가 아니라면 Git 작업을 시도하거나 저장소를 임의로 초기화하지 않고 사용자에게 알린다.
7. 관련 없는 기존 변경을 삭제하거나 덮어쓰지 않은 상태에서 작업한다.

사용자가 즉시 실행을 명시한 작업은 그 요청을 작업 자체에 대한 승인으로 볼 수 있다. 다만 TODO 등록 위치는 작업 전 메시지로 분명하게 제안하고, 사용자가 다른 순서를 지정하면 즉시 반영한다.

## 5. TODO 관리 규칙

- TODO와 관련된 모든 작업 전에 `docs/TODO_GUIDE.md`를 다시 확인한다.
- 사용자와 논의하여 실행하기로 결정된 모든 작업은 `TODO.md`에 기록한다.
- 단순한 아이디어, 질문 또는 결론이 나지 않은 선택지는 합의된 작업으로 취급하지 않는다.
- 새 작업은 먼저 미대응 섹션에 번호 목록으로 등록한다.
- 등록 전 기존 작업과의 의존성, 긴급도, 사용자 가치 및 위험을 기준으로 우선순위를 판단한다.
- 사용자에게 작업 제목, 구체적인 범위와 함께 “미대응 N번에 넣어도 될까요?”라고 제안한다.
- 작업에 착수하기 전에 해당 항목을 진행중 섹션으로 이동한다.
- 진행중 섹션은 불렛 목록을 사용하며 현재 진행 상황을 구체적으로 기록한다.
- 작업이 중단되면 완료된 부분, 남은 부분, 중단 원인 및 재개 조건을 남긴다.
- 구현과 필요한 검증이 모두 끝난 뒤에만 완료 섹션으로 이동한다.
- 각 항목에는 한 줄 작업 설명과 들여쓴 구체적인 설명이 반드시 있어야 한다.
- 상태 이동과 형식의 상세 규칙은 `docs/TODO_GUIDE.md`를 단일 기준으로 삼는다.

## 6. Git 작업 규칙

Git 관련 명령을 실행하거나 브랜치, 커밋, PR을 다루기 전에 반드시 `docs/GIT_STRATEGY_S1.md` 전체를 확인한다.

- `main`에 직접 커밋하거나 직접 푸시하지 않는다.
- `develop` 브랜치를 사용하지 않는다.
- 모든 변경은 목적에 맞는 작업 브랜치에서 수행한다.
- 하나의 커밋에는 하나의 논리적인 작업만 포함한다.
- 관련 없는 변경을 같은 커밋이나 PR에 포함하지 않는다.
- 커밋 메시지는 `<영문 prefix>: <한국어 설명>` 형식으로 작성한다.
- PR 제목과 본문은 한국어로 작성한다.
- PR 전 가능한 범위에서 분석, 테스트 및 빌드를 수행하고 결과를 기록한다.
- 검증 실패를 숨기지 않는다.
- 일반 커밋, 일반 푸시, PR 생성 및 병합은 합의된 작업과 검증이 끝나면 별도 사용자 승인 없이 AI Agent가 자동으로 수행한다.
- 기존 미커밋 변경을 임의로 삭제, 복원 또는 정리하지 않는다.
- 강제 푸시, 하드 리셋, 강제 브랜치 삭제 등 파괴적 Git 작업은 사용자 승인 없이 수행하지 않는다.
- 작업 완료 후에는 변경 상태와 현재 브랜치를 다시 확인한다.

Git 저장소가 존재하지 않거나 올바른 작업 브랜치를 만들 수 없는 경우 정책을 우회하여 커밋하지 않는다. 상황과 필요한 다음 조치를 사용자에게 보고한다.

## 7. 언어와 문서 규칙

- 모든 설계 문서, 개발 문서, TODO, Git 설명 및 코드 주석은 한국어로 작성한다.
- 사용자에게 보이는 앱 UI는 일본어로 작성한다.
- 클래스명, 변수명, 함수명, 파일명 등 코드 식별자는 일반적인 개발 관례에 따라 영어를 사용한다.
- 외부 고유명사, API 명칭, 코드 및 명령은 원래 표기를 유지할 수 있다.
- 문서는 현재 동작과 결정 사항을 설명해야 하며, 더 이상 유효하지 않은 설명을 방치하지 않는다.
- 기능 변경이 시스템 설계에 영향을 주면 `docs/SYSTEM_DESIGN_V1.md`를 함께 갱신한다.
- 모든 문서의 제목과 설명은 한국어를 기본으로 하며, 파일명은 프로젝트에서 정한 이름을 유지한다.

## 8. AGENTS.md와 CLAUDE.md 동기화

- 두 파일 중 하나를 변경할 때는 같은 작업에서 다른 파일도 동일하게 변경한다.
- 한 파일만 수정한 상태로 작업을 완료하거나 커밋하지 않는다.
- 변경 후 `cmp -s AGENTS.md CLAUDE.md`와 같은 바이트 단위 비교로 동일성을 검증한다.
- 두 파일의 내용이 다르면 다른 작업보다 먼저 차이를 해소한다.
- 두 파일 사이에 링크, 축약본 또는 도구별 예외 내용을 두지 않는다.

## 9. 제품의 불변 원칙

다음 원칙을 위반하는 기능은 구현하지 않는다.

- Noson은 실제 시장 데이터를 사용하는 암호화폐 현물 모의투자 앱이다.
- 실제 금융거래, 자산 보관, 송금 또는 입출금을 수행하지 않는다.
- 거래소 계정, API Key 또는 지갑을 연결하지 않는다.
- 투자 추천, 종목 추천 또는 수익 보장 기능과 표현을 제공하지 않는다.
- 앱의 자산, 수익 및 손실은 모두 가상 데이터이다.
- 지원 종목은 MVP 기준 `BTC`, `ETH`, `XRP`, `SOL`, `DOGE`이다.
- 기준 통화는 JPY이고 최초 가상자금은 `¥1,000,000`이다.
- 회원가입 없이 익명 Installation ID만 사용한다.
- 일반 데이터와 시장가 주문은 앱에서 처리하고, 앱 종료 중 지정가 주문 처리만 Cloudflare 백엔드가 담당한다.
- 광고, 인앱결제 및 수익화 기능을 사용하지 않는다.
- 개발비와 운영비 `¥0` 목표를 해치는 서비스를 임의로 도입하지 않는다.

상세 기준과 미결정 사항은 `docs/SYSTEM_DESIGN_V1.md`를 따른다.

## 10. 구현 원칙

- Flutter와 Dart의 일반적인 관례를 따른다.
- 상태 관리는 Riverpod, 로컬 데이터는 Drift/SQLite를 기본 선택으로 사용한다.
- 기능 단위의 책임이 드러나는 단순한 구조를 우선한다.
- 테스트 가능성과 책임 분리에 필요한 수준 이상으로 계층이나 추상화를 늘리지 않는다.
- 새로운 패키지나 외부 서비스를 추가하기 전에 필요성, 유지보수 부담, 라이선스 및 무료 사용 가능 여부를 확인한다.
- 시장 데이터 접근과 주문/자산 도메인 로직을 UI 위젯에 직접 결합하지 않는다.
- 금액, 가격, 수량 및 수수료 계산에서 부동소수점 오차가 누적되지 않도록 한다.
- 실제 주문을 거래소로 전송하는 인증 API를 사용하지 않는다.
- 비밀값, API Key 또는 개인 식별 정보를 저장소에 커밋하지 않는다.
- 생성 파일을 직접 수정하기 전에 원본 설정이나 생성 절차를 우선 확인한다.
- 오류를 무시하거나 조용히 삼키지 말고 사용자가 이해할 수 있는 상태와 복구 경로를 설계한다.

## 11. 외부 정보 확인

다음과 같이 변경 가능성이 있는 정보를 구현 또는 출시 판단에 사용할 때는 최신 공식 출처를 확인한다.

- GMO Coin API 명세와 이용약관
- GMO Coin 현물 거래 수수료
- Apple App Store Review Guidelines
- Google Play 금융 기능 관련 정책
- Flutter, Dart 및 사용 패키지의 현재 API
- Cloudflare Workers, D1 및 Cron의 무료 한도와 제약

확인 날짜와 구현에 영향을 준 내용을 관련 문서 또는 작업 기록에 남긴다. 공식 정보가 설계와 충돌하면 임의로 진행하지 않고 사용자와 범위 변경을 결정한다.

## 12. 검증 원칙

변경 위험에 비례하여 다음 검증을 수행한다.

- 문서: 내용 누락, 상호 모순, 링크, 형식 및 중복 확인
- Dart 코드: 포맷, 정적 분석 및 관련 단위/위젯 테스트
- 데이터베이스: 스키마, 마이그레이션 및 기존 데이터 보존 확인
- 주문/자산 계산: 정상, 경계값, 오류 및 중복 처리 테스트
- 네트워크: 오류, 타임아웃, 재연결 및 잘못된 응답 처리 확인
- UI: 일본어 문구, 주요 상태 및 대상 플랫폼 렌더링 확인
- 백엔드: 멱등성, 체결 조건, 취소 경합, Cron 및 저장 실패 확인

검증할 수 없는 항목은 완료로 간주하지 말고 이유와 위험을 명시한다.

## 13. 완료 기준과 보고

작업은 다음 조건을 만족할 때 완료된다.

1. 합의된 범위가 구현 또는 문서에 반영되었다.
2. 관련 테스트, 분석 또는 수동 검증이 수행되었다.
3. 관련 문서가 현재 상태와 일치한다.
4. `AGENTS.md`와 `CLAUDE.md`를 변경했다면 두 파일이 완전히 같다.
5. TODO 항목이 완료 섹션으로 이동했고 구체적인 결과와 검증 내용이 기록되었다.
6. Git 저장소가 있다면 정책에 맞는 작업 브랜치와 논리적 커밋 상태를 확인했다.
7. 남은 위험, 실패한 검증 및 후속 작업을 사용자에게 숨김없이 보고했다.

최종 보고에는 변경한 내용, 검증 결과, Git 상태 및 사용자 판단이 필요한 후속 항목만 간결하게 포함한다.
